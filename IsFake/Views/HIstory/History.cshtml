@model IEnumerable<IsFakeViewModels.HistoryCompareViewModel>

@{
    ViewData["Title"] = "History";
}

<h1>User History</h1>

<table class="table">
    <thead>
        <tr>
            <th>Statement Files</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <ul>
                        @foreach (var statementFile in item.StatementFile)
                        {
                            <li>@statementFile</li>
                        }
                    </ul>
                </td>
            </tr>
        }
    </tbody>
</table>
